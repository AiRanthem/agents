interface:
  display_name: "Implement Design"
  short_description: "Resolve implementation TODOs, then plan or write code"
  default_prompt: "Use $implement-design to understand the approved Why and What, discuss implementation choices, and produce actionable TODOs as the final plan in native Plan mode or execute them in Agent mode."
policy:
  allow_implicit_invocation: true
