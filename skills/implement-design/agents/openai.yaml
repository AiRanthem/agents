interface:
  display_name: "Implement Design"
  short_description: "Implement confirmed designs exactly and verify them"
  default_prompt: "Use $implement-design to implement a confirmed design document exactly and validate the result."
policy:
  allow_implicit_invocation: true
