[
{"file":"pkg/sandbox-manager/core.go","line":180,"summary":"Leader elector started once with no re-acquire loop; vendored LeaderElector.Run returns on lease loss, so a replica that loses leadership is stuck primary=false until restart.","failure_scenario":"replicas:1; the sole leader hits a ~10s apiserver/network blip, renew() fails -> Run() returns -> OnStoppedLeading sets primary=false. core.go:180 (and leader.go:126) call Run exactly once with no for-loop, so IsPrimary() stays false forever. AntiDriftDriver.RunOnce and releaseWithProbe early-return on !isPrimary; leaked Redis q:live entries are never reconciled -> spurious 429s that never self-heal until pod restart."},
{"file":"pkg/servers/e2b/quota/manager.go","line":58,"summary":"backendErrorsTotal{operation=\"acquire\"} is double-incremented: RedisBackend.Acquire already increments at redis.go:80 before returning ErrBackendUnavailable, then Manager.Acquire increments again.","failure_scenario":"Redis unreachable, limited key calls Acquire. redis.go:79-81 increments backendErrorsTotal[acquire] then returns wrapped ErrBackendUnavailable; manager.go:57-58 increments it a second time. e2b_quota_backend_errors_total{operation=acquire} reads 2x on every fail-open acquire, corrupting the primary Redis-health alert in a fail-open design."},
{"file":"pkg/servers/e2b/quota/antidrift.go","line":211,"summary":"Remove-gate age check now.Sub(acquiredAt) mixes clock sources: now is the pod's Go clock, acquiredAt is the Redis server clock (Acquire Lua) or apiserver clock (AddObserved); spec §6.4.2 requires a single Redis clock.","failure_scenario":"Pod clock skewed ahead of Redis by S seconds. A leaked entry whose true age is grace-epsilon is seen as (grace-epsilon)+S, so when S>epsilon it is HDEL'd up to S seconds early (or removal is deferred if pod clock lags). Bounded by NTP skew vs 10min grace (low practical impact), but the grace boundary is not honored as specified and degrades if NTP drifts."},
{"file":"pkg/servers/e2b/services.go","line":79,"summary":"DeleteSandbox issues a Redis HDEL Release for every delete including unlimited keys, breaking the spec's 'unlimited keys do zero Redis IO on the hot path' guarantee.","failure_scenario":"Redis configured; unlimited key deletes a sandbox. Every create path now stamps AnnotationLock, so owner+lockString are non-empty and Release runs an HDEL that removes nothing. At ~2500 deletes/sec the majority (unlimited) each add one wasted Redis round-trip; create side short-circuits unlimited keys but delete side does not. Gate on user.QuotaSpec.IsLimited() (user == owner here)."},
{"file":"pkg/servers/e2b/quota/antidrift.go","line":312,"summary":"sandboxLiveForQuota duplicates cache.isLiveForQuota byte-for-byte instead of reusing the spec's 'single source of truth' predicate.","failure_scenario":"antidrift.go:312 and cache.go:388 have identical bodies. A future change to the live predicate must be made in both files or the event-release path silently diverges from the count read and anti-drift add/remove. Export one cache.IsLiveForQuota and call it from the quota layer."},
{"file":"pkg/servers/e2b/quota/redis.go","line":194,"summary":"redisInt64 hand-reimplements the type coercion that vendored go-redis (*redis.Cmd).Int64() already provides.","failure_scenario":"releaseScript.Run(...) returns a *redis.Cmd that exposes .Int64(); the 15-line redisInt64 helper plus the .Result()+switch can be replaced by redisReleaseScript.Run(...).Int64(). Duplicated maintenance, no behavior change."},
{"file":"pkg/sandbox-manager/infra/sandboxcr/claim.go","line":316,"summary":"quotaReleaseTimeout (250ms) is defined verbatim in two packages and the WithoutCancel+timeout context pattern is copy-pasted three times.","failure_scenario":"const quotaReleaseTimeout is duplicated at claim.go:316 and create.go:52; context.WithTimeout(context.WithoutCancel(ctx), quotaReleaseTimeout) recurs at claim.go:329, create.go:81, api_key.go:179. The two constants can drift independently. Extract one shared helper/constant."},
{"file":"pkg/sandbox-manager/infra/sandboxcr/clone.go","line":168,"summary":"chooseCloneAttemptLockString and chooseAttemptLockString are identical logic differing only in the options struct type.","failure_scenario":"claim.go:318 and clone.go:168 have the same two-line body; the same pattern is also inlined in both ValidateAndInit* functions. Replace with one func(admission *infra.SandboxAdmission, lockString string) string used by both flows."},
{"file":"pkg/servers/e2b/quota/antidrift.go","line":227,"summary":"Event UpdateFunc/DeleteFunc allocate a 5s context and run on every Sandbox event on every replica before the isPrimary() check (deferred to releaseWithProbe line 262).","failure_scenario":"At high churn every non-leader replica's informer fires UpdateFunc per event, each allocating context.WithTimeout(context.Background(),5s) and incrementing the event_not_primary skip counter. Check the cheap isPrimary() first to avoid per-event allocation and an inflated follower counter."},
{"file":"pkg/servers/e2b/api_key.go","line":174,"summary":"cleanupQuotaForDeletedAPIKey issues a Redis DEL (with retry/backoff goroutine) for every deleted key, including unlimited keys that never had a live set.","failure_scenario":"Deleting an unlimited key spawns a goroutine running DEL q:live:{K} against a never-existing key. Harmless and bounded (2s WithoutCancel context, not a leak) but a needless Redis op; the call site holds the key's QuotaSpec and can gate on IsLimited() (spec §6.6 only requires DEL for limited keys)."},
{"file":"pkg/servers/e2b/core.go","line":44,"summary":"The local quotaManager interface re-declares *quota.Manager's exact public method set for no abstraction gain.","failure_scenario":"quotaManager{Acquire,Release,DeleteSubject} mirrors *quota.Manager, the only type ever assigned to sc.quota, and is not used as a production test seam. It adds a parallel signature to keep in lockstep. Use *quota.Manager directly or move the interface into the quota package."},
{"file":"pkg/servers/e2b/models/quota.go","line":129,"summary":"APIKeyQuotaFromSpec is a thin error-swallowing wrapper over APIKeyQuotaFromSpecChecked that re-runs NormalizeQuotaSpec on callers that already normalized.","failure_scenario":"Secret/MySQL read paths (toCreatedTeamAPIKey, marshalAPIKeyForStorage, storeKey, ListByOwnerTeam) call APIKeyQuotaFromSpec, which re-validates+copies a spec they just normalized, and silently drops errors. Build the public shape directly from SandboxCountLimit() when the spec is already normalized, keeping only the Checked variant."},
{"file":"pkg/servers/e2b/quota/antidrift.go","line":228,"summary":"Event-driven releases use context.Background() with only a 5s timeout, so they are not tied to the driver lifecycle and Stop()/shutdown cannot cancel in-flight event releases.","failure_scenario":"Driver.Stop() during shutdown cannot cancel an in-flight Delete-event release because its context derives from Background; the probe + backend.Release continue against Redis for up to 5s. Bounded and idempotent, so low severity, but the event handlers ignore the driver's run context."}
]